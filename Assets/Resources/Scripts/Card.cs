
namespace LuckGame {
    public class Card : CardBase
    {
        public Card(int cardId, int cardType, string cardName, string cardDesc, int cardRare) : base(cardId, cardType, cardName, cardDesc, cardRare)
        {
        }
    }
}